module.exports = class Ng {
  constructor () {
  }

  /*
   * convert data to real react component
   * based on components provided by themes
   *
   * @param {Path} datadir the path to the data file
   * @param {Path} themedir the path to the theme file
   * @return {Component} the final component based on data passed in
   */
  start (datadir, themedir) {
    // TODO
  }
}
